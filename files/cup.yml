services:
  cup:
    image: ghcr.io/sergi0g/cup:latest
    container_name: cup # Optional
    restart: always
    command: serve
    ports:
      - 5002:8000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock